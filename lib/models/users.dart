class User {
  String name;
  String birthday;
  String address;
  String username;
  String password;
  String avatarUrl;

  User(this.name, this.address, this.avatarUrl, this.birthday, this.password, this.username);
}
